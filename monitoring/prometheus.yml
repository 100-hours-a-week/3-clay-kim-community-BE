global:
  scrape_interval: 1s
  evaluation_interval: 5s

scrape_configs:
  - job_name: community-app
    metrics_path: /api/actuator/prometheus
    static_configs:
      - targets:
          - app:8080
